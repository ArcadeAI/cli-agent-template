{
  "name": "chat-agent-template",
  "module": "agent.ts",
  "type": "module",
  "private": true,
  "version": "0.0.1",
  "description": "Chat Agent Template",
  "peerDependencies": {
    "typescript": "^5"
  },
  "bin": {
    "agent": "./agent.ts"
  },
  "scripts": {
    "format": "prettier --write .",
    "lint": "prettier --check .",
    "compile": "bun build agent.ts --compile --outfile agent",
    "clear": "rm -rf .context/arcade"
  },
  "dependencies": {
    "@commander-js/extra-typings": "^14.0.0",
    "@openai/agents": "^0.4.11",
    "chalk": "^5.4.1",
    "commander": "^14.0.0",
    "openai": "^6.20.0",
    "ora": "^8.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.6.2"
  }
}
